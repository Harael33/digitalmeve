# au lieu de tout sur une ligne trop longue
return {"filename": filename, "size": size, "mime": mime, "sha256": sha256}

# si besoin, casse comme ça
return {
    "filename": filename,
    "size": size,
    "mime": mime,
    "sha256": sha256,
}
