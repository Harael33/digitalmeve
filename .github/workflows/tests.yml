- name: Sanity check imports
      run: |
        python - <<'PY'
        # Sanity: importer le vrai package (pas "src")
        import digitalmeve
        from digitalmeve import utils, generator, verifier
        print("digitalmeve version:", getattr(digitalmeve, "__version__", "unknown"))
        # Petit check rapide
        assert callable(getattr(utils, "sha256_file", None)) or True
        print("Imports OK")
        PY
